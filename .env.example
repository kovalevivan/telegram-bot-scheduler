# Optional. If not set, service uses local SQLite file: sqlite+aiosqlite:///./data/app.db
# Example for Postgres (recommended for horizontal scaling):
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname

# Worker settings
WORKER_POLL_SECONDS=30
WORKER_BATCH_SIZE=200
WORKER_LOCK_LEASE_SECONDS=120
MAX_CONCURRENT_RUNS=100

# HTTP settings
PUZZLEBOT_BASE_URL=https://api.puzzlebot.top/
HTTP_TIMEOUT_SECONDS=20
HTTP_RETRIES=2

